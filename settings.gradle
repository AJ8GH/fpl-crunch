pluginManagement {
  plugins {
    id 'io.quarkus' version quarkusVersion
    id 'org.kordamp.gradle.jandex' version jandexVersion
  }
}

rootProject.name = 'fpl-crunch'

include 'api'
include 'client'
include 'service'
